/*
Test with edge inputs.
*/

void main(string[] args) {
    long x = 110l;
    long y = 110L;
    new a{1.1};
    new a{2L};
    new a{1};
    string b = args[1];
    new c{1}
    new c{1.1}
    new c{1L}
    string s = true + " or false";
    for (int i = 0; i < 3; i++){
        for (int j = 0; j < 3; ++j){
            string cur = "first: " + i + " & second: " + j;
            println(cur);
        }
        string cur2 = "done first: " + i;
        println(cur2);
    }
}

struct a {
    double b;
};

struct c {
    int a;
}